# 026 - ElastiCache Redis

Managed Redis. Helpers create/describe/delete clusters and build Redis URLs/cache keys.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci: wire-only. Real AWS: subnet groups, auth token, TLS, eviction policy, replication, alarms.
