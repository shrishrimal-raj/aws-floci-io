# 039 - Route53

DNS, hosted zones, records, routing policies. Helpers create zones, upsert A records, list/delete zones, build FQDNs.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci partial. Real AWS: health checks, weighted/latency records, DNSSEC, delegation.
