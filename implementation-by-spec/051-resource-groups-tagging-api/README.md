# 051 - Resource Groups Tagging API

Tag-based resource queries across services. Helpers query resources by tag, tag/untag resources, standard tags.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci full. Real AWS: enforce tag policies, cost allocation tags, remediation, org-wide inventory.
