# 047 - CloudFront

CDN with origins, behaviors, invalidations, OAC/signed URL patterns. Helpers get distributions, invalidate paths, shape configs.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci partial. Real AWS: TLS, cache policies, OAC, WAF, logs, invalidation cost.
