# 030 - EKS

Managed Kubernetes with IRSA/addons. Helpers create/describe/list/delete clusters and build cluster ARN names.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci: partial. Real AWS: VPC CNI, node groups, IAM/OIDC, addons, upgrades, observability.
