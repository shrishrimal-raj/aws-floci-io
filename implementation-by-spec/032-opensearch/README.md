# 032 - OpenSearch

Managed search: BM25, kNN, dashboards. Helpers create/describe/list/delete domains and build index mappings.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Real AWS: VPC domains, fine-grained access, snapshots, shard sizing, slow logs, ISM policies.
