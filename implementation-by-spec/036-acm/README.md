# 036 - ACM

TLS certificate management. Helpers request DNS certs, list/describe/delete, build wildcard names.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci stub. Real AWS: DNS validation, regional certs, CloudFront us-east-1 certs, renewal monitoring.
