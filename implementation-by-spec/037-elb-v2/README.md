# 037 - ELB v2

ALB/NLB load balancing. Helpers create ALBs/target groups and cleanup; path rule helper.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci partial. Real AWS: listeners, TLS, health checks, access logs, WAF, target draining.
