# 043 - AppConfig

Feature flags + dynamic config with validation. Helpers create application/env/profile, deploy configs, build flag payloads.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci partial. Real AWS: validators, deployment strategies, bake time, rollback alarms, agents.
