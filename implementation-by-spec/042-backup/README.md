# 042 - AWS Backup

Centralized backup policies. Helpers create daily plans, list plans, start backup jobs, delete plans.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci partial. Real AWS: vault lock, cross-region copy, retention, restore drills, compliance.
