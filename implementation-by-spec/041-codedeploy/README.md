# 041 - CodeDeploy

Deployment automation: blue/green, canary, in-place. Helpers create apps/groups/deployments and appspec.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci partial. Real AWS: deployment configs, hooks, rollback alarms, traffic shifting.
