# 035 - CloudFormation

Infrastructure as code: stacks, updates, change sets. Helpers create/update/describe/delete stacks and build sample template.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci partial. Real AWS: change sets, drift detection, rollback alarms, nested stacks.
