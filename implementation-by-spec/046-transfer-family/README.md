# 046 - Transfer Family

Managed SFTP/FTPS/FTP fronting S3. Helpers create SFTP servers/users, describe/delete, build S3 homes.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci partial. Real AWS: identity provider, SSH keys, IAM home directory policy, logging.
