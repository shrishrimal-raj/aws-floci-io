# 029 - EC2

Virtual machines, AMIs, instance types, security groups. Helpers create SGs, run/describe/terminate instances, user data.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci: partial. Real AWS: least-open security groups, SSM access, AMI patching, autoscaling, cost controls.
