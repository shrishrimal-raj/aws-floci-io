# 050 - Pricing API

Programmatic AWS price lookup. Helpers query products, EC2 on-demand filters, parse price list items.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci stub. Real AWS: pricing endpoint regions, SKU parsing, cache results, combine with usage.
