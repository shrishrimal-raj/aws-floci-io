# 048 - AWS Config

Resource compliance + drift detection. Helpers create managed rules, read compliance, shape required-tag rules.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci partial. Real AWS: recorder/delivery channel, aggregators, remediation, conformance packs.
