# 019 - CloudWatch Logs

Log groups, streams, structured events, retention, filtering.

## Run
`pnpm setup && pnpm seed && pnpm test && pnpm cleanup`

Real AWS: retention by default, subscription filters, metric filters, PII redaction, alarms.
