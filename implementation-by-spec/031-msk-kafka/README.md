# 031 - MSK Kafka

Managed Kafka provisioned/serverless patterns. Helpers create/describe/list/delete clusters and build bootstrap strings.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci: partial. Real AWS: subnet sizing, broker storage, auth/TLS, topic replication, consumer lag alarms.
