# 045 - Textract

Extract text, tables, and forms. Helpers detect text, analyze forms/tables, extract line text, build S3 docs.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci partial. Real AWS: async jobs for large PDFs, S3 inputs, SNS/SQS callbacks, human review.
