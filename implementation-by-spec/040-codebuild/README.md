# 040 - CodeBuild

Managed builds with `buildspec.yml`. Helpers create projects, start builds, delete projects, build buildspec text.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci partial. Real AWS: cache, artifacts, reports, VPC builds, secrets, IAM.
