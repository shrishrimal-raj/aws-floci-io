# 028 - ECS

Container orchestration for Fargate/EC2. Helpers create clusters, register Fargate task definitions, run tasks, cleanup.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci: partial. Real AWS: VPC networking, IAM roles, logs, autoscaling, deployments, health checks.
