# 049 - Cost Explorer

Query cost and usage. Helpers get monthly cost, group by service, build month ranges.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci stub. Real AWS: billing permissions, tagging strategy, budgets, anomaly detection, CUR for detail.
