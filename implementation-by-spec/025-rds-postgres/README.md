# 025 - RDS Postgres

Managed Postgres. Helpers create/describe/delete instances and build connection strings.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci: wire-only. Real AWS: private subnets, backups, replicas, RDS Proxy, parameter groups, migrations.
