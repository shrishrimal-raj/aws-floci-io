# AI Document Intelligence Backend

Upload PDF → Textract async → Bedrock summarization → store in OpenSearch with vector embeddings → query via REST. CloudFront fronts the upload bucket.

See `spec-by-phases/phase-09-ai-specialized/README.md` for the full spec.
