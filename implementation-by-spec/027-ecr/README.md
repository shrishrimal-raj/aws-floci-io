# 027 - ECR

Container registry. Helpers create repos, lifecycle policy, auth token, list images, delete repos.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Real AWS: scan on push, immutable tags, lifecycle cleanup, cross-account policy, signing.
