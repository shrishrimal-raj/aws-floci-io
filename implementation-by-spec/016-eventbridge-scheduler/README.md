# 016 - EventBridge Scheduler

Modern cron/one-shot schedules with targets and flexible windows.

## Run
`pnpm setup && pnpm seed && pnpm test && pnpm cleanup`

Real AWS: least-privilege target role, DLQ/retry policy, timezone, one-shot cleanup.
