# 044 - Bedrock Runtime

Invoke foundation models. Helpers build Claude request bodies and invoke JSON models.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci stub. Real AWS: model access, token/cost limits, retries, guardrails, prompt logging policy.
