# 022 - SES v2

Modern SES API with configuration sets and contact lists.

## Run
`pnpm setup && pnpm seed && pnpm test && pnpm cleanup`

Real AWS: event destinations, suppression lists, contact topics, reputation monitoring.
