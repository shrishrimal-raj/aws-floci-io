# 023 - Kinesis Data Streams

Real-time streams with shards, partition keys, put/get records.

## Run
`pnpm setup && pnpm seed && pnpm test && pnpm cleanup`

Real AWS: shard math, enhanced fan-out, checkpointing, retries, ordering per partition key.
