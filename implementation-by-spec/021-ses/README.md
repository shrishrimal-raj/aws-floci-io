# 021 - SES

Transactional/bulk email via SES v1 API.

## Run
`pnpm setup && pnpm seed && pnpm test && pnpm cleanup`

Real AWS: verify domains, DKIM/SPF/DMARC, bounce/complaint handling, sandbox exit, templates.
