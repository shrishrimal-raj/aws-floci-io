# 034 - Glue

Data catalog + ETL metadata. Helpers create databases/tables, lookup and cleanup catalog objects.

Run: `pnpm setup && pnpm seed && pnpm test && pnpm cleanup`.
Floci partial. Real AWS: crawlers/jobs, partitions, schema evolution, IAM lake permissions.
