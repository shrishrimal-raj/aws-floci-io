# 018 - Step Functions

Workflow orchestration with ASL, Standard/Express patterns.

## Run
`pnpm setup && pnpm seed && pnpm test && pnpm cleanup`

Real AWS: model retries/catches, idempotency, execution history, logging, X-Ray, quotas.
