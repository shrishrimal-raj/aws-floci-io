# 015 - EventBridge

Event bus, rules, targets, and event publishing.

## Run
`pnpm setup && pnpm seed && pnpm test && pnpm cleanup`

Real AWS: archive/replay, DLQs, schema registry, cross-account bus policies, target retries.
